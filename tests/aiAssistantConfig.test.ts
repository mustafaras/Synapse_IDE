/**
 * Tests moved to src/tests/aiAssistantConfig.test.ts so Vite/TS can bundle them.
 * Run tests via the in-app harness:
 *   - npm run test:dev   # opens dev server with ?tests=1&autorun=1
 *   - npm run test:preview
 * Or manually open: http://localhost:5173?tests=1
 */
describe('placeholder suite', () => {
	it('smoke', () => {
		expect(true).toBe(true);
	});
});
